# FastAPI และ dependencies หลัก
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Face Recognition และ Computer Vision
face-recognition==1.3.0
opencv-python==4.8.1.78
Pillow==10.1.0
numpy==1.24.3

# Database และ Supabase
supabase==2.0.0
postgrest==0.13.0

# Utility libraries
pydantic==2.5.0
requests==2.31.0
python-jose[cryptography]==3.3.0

# หมายเหตุ: หากติดตั้งไม่ได้บน Windows อาจต้องติดตั้ง dlib แยก
# สำหรับ Windows: pip install dlib 
# หรือดาวน์โหลด wheel file จาก https://github.com/ageitgey/face_recognition/issues/175

# สำหรับ macOS: อาจต้องติดตั้ง cmake และ dlib ก่อน
# brew install cmake
# pip install dlib

# สำหรับ Linux: อาจต้องติดตั้ง development packages
# sudo apt-get install build-essential cmake
# sudo apt-get install libopenblas-dev liblapack-dev 
# sudo apt-get install libx11-dev libgtk-3-dev